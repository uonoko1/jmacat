"""Use case layer: interactors that orchestrate a single task.

Standard library only; it depends on `jmacat.domain` and on the ports it
declares, never on `jmacat.infrastructure` or `jmacat.controller`.
Enforced by `tests/test_architecture.py`.
"""

"""Domain layer: record layout, unit conversion, filter predicates.

Standard library only. This layer must not import any third-party package,
nor `jmacat.infrastructure` or `jmacat.controller`. Enforced by
`tests/test_architecture.py`.
"""
